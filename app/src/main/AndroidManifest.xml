<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.strauss_teste1">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Straussteste1"
        tools:targetApi="31">

        <!-- Tela Inicial: Tipo de Usuário -->
        <activity
            android:name=".TipoUsuarioActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Telas de Login -->
        <activity android:name=".LoginAlunoActivity" android:exported="true"/>
        <activity android:name=".LoginPersonalActivity" android:exported="true"/>
        <activity android:name=".LoginActivity" android:exported="true"/>

        <!-- Tela de Cadastro -->
        <activity android:name=".CadastroActivity" android:exported="true" />

        <!-- Telas do Aluno -->
        <activity android:name=".HomeAlunoActivity" android:exported="true" />
        <activity android:name=".MainActivity" android:exported="true" />
        <activity android:name=".AgendamentosActivity" android:exported="true" />
        <activity android:name=".FeedbackActivity" android:exported="true" />

        <!-- Telas do Personal -->
        <activity android:name=".HomePersonalActivity" android:exported="true" />
        <activity android:name=".AgendamentosPersonalActivity" android:exported="true" />
        <activity android:name=".FeedbackPersonalActivity" android:exported="true" />

    </application>

</manifest>
